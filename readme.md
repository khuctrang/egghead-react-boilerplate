# Custom Webpack Loader

![alt text](scrcap/scrcap-1.png)

What it does?

- The loader scans all img tags in jsx using regex.
- Then, hash the image content and add the hash to the query string in paths.
